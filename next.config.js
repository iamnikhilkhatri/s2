/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
module.exports = { images: { domains: ["images.ctfassets.net"] } };

// const withImages = require("next-images");

// module.exports = withImages({
//   images: {
//     formats: ["image", "video"],
//   },
// });
